<%= turbo_frame_tag dom_id(@comment, :buttons) do %>
  <% if @comment.author_id == current_user.id %>
    <%= button_to "🗑 Delete", post_comment_path(@post_id, @comment.id), method: :delete %>
    <% if @comment.parent_id == nil %>
      <%= link_to "Edit", edit_post_comment_path(@post_id, @comment.id) %>
    <% else %>
      <%= link_to "Edit",
      edit_post_comment_path(@post_id, @comment.id, parent_id: @comment.parent_id) %>
    <% end %>
  <% end %>
<% end %>
